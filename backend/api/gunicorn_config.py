bind = "0.0.0.0:5000"
workers = 2  # Adjust this number based on your server's CPU count
threads = 4  # Adjust the number of threads based on your needs
timeout = 120
accesslog = "-"  # Log access to stdout
errorlog = "-"  # Log errors to stdout
